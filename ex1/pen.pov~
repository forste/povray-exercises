#include "colors.inc"

background {
  color White
}
camera {
  location <0, 3, -3>
  look_at  <0, 0,  0>
}

light_source {
  <-2, 3, 2>
  color White
}

cylinder {
      <0, 0, 0>,     // Center of one end
      <1, 0, 3>,     // Center of other end
      0.5            // Radius
      open           // Remove end caps
      texture { T_Stone25 scale 4 }
}